@model IEnumerable<Tekliftakip.Models.Stock>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Yeni Ekle</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.StockCode)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.StockAmount)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.StockCode)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.StockAmount)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.StockId">Düzelt</a> |
                <a asp-action="Details" asp-route-id="@item.StockId">Detay</a> |
                <a asp-action="Delete" asp-route-id="@item.StockId">Silme</a>
            </td>
        </tr>
}
    </tbody>
</table>
